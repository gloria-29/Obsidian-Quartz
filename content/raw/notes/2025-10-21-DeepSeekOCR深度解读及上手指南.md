---
标题: DeepSeekOCR深度解读及上手指南
笔记 ID: 019a06f0-7864-754d-a0a9-41c5fb70d86c
笔记类型: crawl
卡片盒:
  - 乐趣
包含语音: 是
网页链接: null
创建时间: "2025-10-21T21:23:34.117Z"
更新时间: "2026-03-03T14:07:29.072Z"
---
![录音](https://dinox-gloria29.s3.bitiful.net/dinox_note/assets/019126f0-2672-7286-b640-feeb1616b906/videos/019a06ef-e287-7784-b1ae-d5649a66d8a8.mp4#61;AWS4-HMAC-SHA256&amp;X-Amz-Content-Sha256&#61;UNSIGNED-PAYLOAD&amp;X-Amz-Credential&#61;OEifDUYHchAg0XKa4VK8vug4%2F20251021%2Fcn-east-1%2Fs3%2Faws4_request&amp;X-Amz-Date&#61;20251021T132309Z&amp;X-Amz-Expires&#61;86400&amp;X-Amz-Signature&#61;11c6206ca84d3a2958b966140e605cec96b0168cc67b298adbf2e8fd0f5c3a3a&amp;X-Amz-SignedHeaders&#61;host&amp;x-amz-checksum-mode&#61;ENABLED&amp;x-id&#61;GetObject)


DeepSeek-OCR深度解读+上手指南！高精度表格、公式、CAD图字符&amp;语义混合识别，多模态PDF一键转化MarkDown！多模态混合DeepSeek实战,科技猎手,AI,人工智能,教程,多模态RAG,大模型,OCR,rag,多模态,DeepSeek,人工智能,哔哩哔哩,bilibili,B站,弹幕

作者：九天Hector

### 视频转录原文
多模态版deep sik新模型终于来了，不到3B的尺寸，仅需7G的显存，就能够完成高精度的表格公式识别、图片语义识别、视觉元素分割，多模态PDF到markdown文档一键转换等各项工作，并且在多项评测指标中一举拿下sota成绩。这就是deep sick模型模型模模型Dept KOCR。可以说在多模态识别日益重要的今天，deep sig OCR模型不仅提供了一整套目前性价比最高的图像多模态识别的解决方案，而且能几乎零门槛的介入到多模态rep系统开发等多项重要的开发场景中。并且根据模型原论文描述，Dept CKOCR还首次实现了情境光学压缩，从而能够采用更少的视觉标记来获得更好的视觉识别效果。因此其他程度上解决了开源多模态模型尺寸往往较大，从而难以部署使用的问题。同时这也将是未来deep sik系统模型的多模态的功能雏形。可以说deep sig OCR模型在理论和实践两个方面都取得了重大的突破。而对于目前很多正在苦苦寻找多模态PDF识别、表格公式识别、CAD图纸识别和检索的解决方案同学来说，春天来了，deep sit OCR模型方案可以说是目前最佳的开源方案，没有之一。本期视频就为你深度解读deep sit OCR模型的核心功能特性，并带大家快速上手部署使用deep sit OCR模型。模型部署调用的文字版教程，deep sit OCR模型的权重测试文件，以及更多基于deep sc COCR开发的多模态Rex系统教程都已经上线。制的CO模型进入社区，大家扫码即可领取关键提醒。本期视频内容较多，总共分为三个部分，分别是dep sit OCR模型的核心功能介绍，deep sig OCR和同类模型的横向对比，以及deep sit OCR模型快速上手指南。每一部分开始的时间如图所示，大家可以根据自己的实际需求跳转观看OCR全程。Dep sit collector OCR模型快速字符识别特指在图像中对文本进行检测和识别。这是一项最早诞生于上世纪50年代的技术，而一直到2010年之后，深度学习的兴起才得到了长足的发展。不过早些年基于传统的深度学习的OCR技术，往往是采用的是CN加LSTM类型的解决方案，只能对图片中的文本进行扫描和识别。典型的如百度飞桨OCR能够很好的完成手写体、文本、表格、公式等对象的识别和转化，但是其无法真正的理解图片真实的语义。因此对于一些装饰图流程图或者CAD图纸等，这些OCR模型是无法识别的。但是现在大家看到的deep sig OCR，尽管也顶着一个OCR的名字，但却不是传统的OCR模型，而是拥有大模型多模态能力的OCR模型。这种模型被称为VLL模型或者是OCR2.0模型。而其中最本质的区别就是多模态大模型在进行图像识别之前，会借助一个名叫视觉编码器的算法，将图像视觉信息映射到文本空间中。然后再借助大模型对文本语义的理解能力，间接的去理解图像信息。而deep sig OCR模型作为OCR2.0模型，自然也提出了自己的视觉编码器deep encoder。基于此，deep sig OCR模型不仅能够顺利的识别图像中的文字，也能够真正的理解图像的语义。例如对于房屋结构CAD图纸，deep sick就能够很好的识别其含义和语义信息，并且效果非常不错。也就是说deep sig OCR实际上就是一个顶的OCR名字的多模态大模型。而其中的deep encoder相信也将会是未来全新一代deep sc ER模型的多模态模型的模型。值得一提的是，作为国产开源之光，deep si KOCR开源的同时也公布了deep encoder的核心架构逻辑。我们可以在模型权重文件夹中找到，也可以在deep公coder的课件网盘中直接下载。当然了，实现多模态功能本身并不稀奇，但如何实现，实现到什么程度才是关键。总的来说DPCOCR模型就是为了目前多模态信息检索量身定制的大模型。首先deep sit OCR能够高效准确的识别多模态PDF文档格式，准确的划分如标题、正文、图片、表格、公式等等信息。并且DPCOCR也能够一键将PDF也直接转化为markdown文档。同时也能对PDF中的图像进行高精度的语义识别分析。而对于图片中的实体或者图片中隐晦的语义，DPCOCR模型也能够顺利的识别。而对于开发者，你只需要编写不同的提示词即可引导deep sit OCR模型完成各项工作，是不是非常高效便捷。可以说deep sik OCR模型就是为了多模态信息检索，多模态read系统量身定制的大模型。而如果和同类型的模型做横向对比相比，panda OCR deep sig OCR能够围绕图像的视觉信息进行检索，从而实现类似CAD图纸的识别检索等功能。而如果相比传统的通用的VM多模态大模型，如千万3VL deep sig OCR模型的视觉编码器则是经过了专门的微调，能够更好的进行如多模态PDF1键转换markdown等各项任务。而相比其他的经过微调的OCR2.0模型，比如说OLMOCR、Intel VLL或者miner等等，deep sig OCR模型则拥有一个非常大的优势，那就是尺寸更小，效果更好，效率更高。而这也正是Depp IICSC模型的最大的一项技术模型。情境光学模型简单理解就是采用更少的像素点来压缩图像信息，然后借助大模型天然的推理能力来补足可能因为压缩带来的逻辑关系的缺失。也正因为如此，PPCCOCR模型才能够以3B的尺寸达到超越Intel 3 78B模型的效果。而除了效果出众外，deep sik OCR模型的推理效率也非常惊人。根据官网说明，模型在单卡A140G上运行的时候能达到2500 tokens每秒。而如此高效率推理速度，完全能够满足真实企业场景下多模态文档识别和转化的需求。而在我实际使用测试的时候，单卡4090也达到了450 token s每秒的输出速度。总的来说deep sc CORR模型和其他OCR模型的各方面对比评测如图所示。好了，在了解了dpc OCR模型的核心特性之后，接下来就让我们看看如何上手来使用。目前DPSCCOCR模型并没有提供在线的API进行调用，因此要用的话需要本地的话要注意的是DPCOCR总参数量约为3B实测部署运行需要占用7B的右的显存，运行时可以使用VLM或者HUAGINEE的Transformer Co进行推理。不过需要注意的是，如果是使用VM进行推理的话，受限于其版本，不能使用最新的50系显卡。视频中均以4090显卡为例进行的演示。首先我们可以在hacky face或者模型设计上下载模型权重，也可以直接扫码领取课件，在网盘中下载模型权重。下载完成后，接下来需要进行环境配置，并使用官方编写的运行脚本进行快速的功能测试。这里我们需要先在命令行中输入deep co命令，下载deep sc TOCR的项目包，然后我们创建一个虚拟环境来安装模型运行的相关依赖。同样是在命令行中输入conduct create命令即可创建一个名为deep sick OCR的虚拟环境。然后我们输入conduct activate deep sick OCR即可激活虚拟环境。然后在虚拟环境中先安装Python ch然后再安装VM。这是一个企业级高并发的大模型推理加速框架，同时也是DPCOCR官方推荐使用的框架。不过安装需要注意，由于基础依赖的不同，我们需要安装指定版本的VRM才能够运行deep sit OCR模型。这里我们需要在网盘中下载特定版本的VM预编译的二进制安装包，并将其上传到服务器中。然后进入到对应的文件夹里边，输入pip install命令来进行安装。接下来让我们进入到GitHub项目主目录，输入pin in命令来安装项目基础依赖。需要注意的是啊，安装过程如果出现了如图所示的依赖冲突，无视即可，不会影响实际运行。最后还需要安装flash attention加速库，至此环境配置相关工作就全部完成了，我们接下来就可以开始调用DFSICOCR模型了。目前deep COCR模型官方提供了三类任务的运行脚本，分别是图片识别类任务，PDF识别和转化类任务，以及批量处理和性能测试类任务。这里我们简单测试DPCOCR模型的图像识别能力和PDF转化down的转化效果。首先我们尝试给模型输入如图所示的CAD图纸，测试模型能否顺利的进行图像的语义识别。测试图片同样是在网盘中，大家扫码即可领取。而在实际调用DFCOCR模型的时候，我们需要先创建image input和image output两个文件夹，用于存放读取的图片和解析后的结果。这里我们将测试图片放入image input文件夹中，然后我们需要进入到刚刚下载Dept ub项目的的master文件夹里边的VM文件夹里边，找到config点PI文件并修改这四个地方。分别是设置model path为模型下载的路径。然后设置input path和output path文件夹地址作为本轮运行的输入和输出地址。最后设置prt让模型详细介绍一下图片中的内容。设置完成后即可回到deep sic OCRVM目录下运行image脚本。此时脚本会开启VM服务并调用模型，然后输入提示词，此时模型就会根据提示词来进行图片的解析。不过需要注意的是啊，DPCOCR模型的中文提示效果不太稳定，如果可以的话，建议以英文提示为主。运行完成后即可在image output文件夹中查看CAD图纸的OCR识别效果。能够发现deep sic OCR模型能够非常准确的识别出当前图纸的各项参数细节，能够完整的实现基于图片语义的理解任务。接下来让我们再尝试来借助deep sit OCR模型，一步到位将多模态的PDF转化为markdown文档，这也是OCR场景下非常常见的一类任务了。这里我们准备了一份测试文件，也上传到课件网盘中。这是一份我的公开课的课件，其中包含了文字、图片、表格等各类信息。而要如何将其转化为markdown文档呢？然后还是需要创建一个PDF input和PDF output的两个文件夹，然后将测试文档上传到PDF input文件夹中，然后还是回到DPCC项目的主目录内修改conflict点PL文档。此时需要注意修改文件的输入和输出的路径，并且需要修改提示词，提示模型，将PDF转换为markdown文档。然后我们输入如下命令，运行PDF解析脚本。此时Dept ITOCR就会先识别PDF中各元素的位置，然后对图片进行单独保存，然后对表格和公式进行识别，最后将所有的元素再拼接为一个完整的markdown文档。运行完成后，即可在PDF杠output文件夹中查看转换后的效果。其中images文件夹中保存了原PDF文档识别出来的图片，而MMD文件就是转换后的markdown文档。能够看出实际转化效果非常不错，和原始文档几乎没有任何区别。而这样的一份markdown文档就可以直接参与到后续的多模态检索过程中了。而如果我们想进一步的在PDF转换为markdown的同时，对图片信息进行OCR字符识别和VM图片语义的混合识别，则需要编写更加复杂提示词和搭建更加复杂的工作流程了。而关于deep si TOCR模型的进阶使用方法，近期我和牧云老师也会开设多场多模态rap系统开发实战公开课。届时会有更多deep sit TORR模型的工具型应用模式识别。欢迎大家扫描型的二维码，进入复大模型技术社区进行免费的学习。好了，以上就是本期视频的工作模式的本期公开课的模型部署调用的文字版教程。Dept TOCR模型权重测试文件，以及更多基于deep CCOCR开发的多模态rap系统教程，都已经上线至复范大模型技术社区，大家扫码即可领取。今年以来大模型技术发展势头非常迅猛，或将持续为大家提供最前沿实用的技术教学。感谢大家关注和三连支持。此外结果还有更多关于我创建的复范大模型技术社区介绍，我们下个视频再见。如果大家看完了前面内容觉得意犹未尽，想要进一步深入系统的学习大模型技术，欢迎大家加入由我和娟娟老师、梦雨老师等几位一线大模型技术人共同创建的复范大模型技术社区。技术人学习不易，很多同学想要入门大模型却不知道从哪儿开始，也有很多同学想要积累项目经验却找不到硬核实战项目。而与此同时，市面上靠着信息差割韭菜的课程比比皆是，有的同学花费上万元报办，但什么都没学会。因此，我们决心打造一个真正硬核并且纯粹公益的学习者的根据地。截至目前，社区已经收录了老师们独家研发的一百多套高质量教程和几10个小时的保姆级教学视频，完整涵盖了大模型从基础到进阶的各个模块。目前已有超过20万开发者加入了社区，社区中的基础教程部分完整涵盖了二十多个主流大模型的部署调用方法，助力大家零基础、快速高效入门。而进阶教程部分则完整涵盖了模型微调、reg知识库检索、agent的开发等核心技术价值对标市场上万元的付费课程。而案例教程部分则更是包括手搓circle agent、deeper research和mini minister等热门方向的高价值实战案例，每一项都是高价值的简历项目，并且社区是纯公益的组织，大模型教程也会持续更新，近一段时间我们就上线了这些最新的前沿硬核干货教程，未来还会持续追踪最新大模型技术内容。如果你想快快速入门模模型，掌握模型核心技术，积累实战项目经验，并且希望持续学习前沿新兴技术，突破职业瓶颈，借助AI升职加薪，欢迎扫码加入复范大模型进入社区，和20万开发者共同学习，一起进步。

&gt;  来自：https://www.bilibili.com/video/BV1eJWUzTECU


